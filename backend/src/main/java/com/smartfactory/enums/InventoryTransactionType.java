package com.smartfactory.enums;

public enum InventoryTransactionType {
    RECEIPT,
    RESERVATION,
    RELEASE,
    CONSUMPTION,
    PRODUCTION,
    WASTE,
    REJECTION,
    ADJUSTMENT,
    TRANSFER
}
