# SmartFactory Environment Variables

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smartfactory_dev
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Spring Boot
SPRING_PROFILES_ACTIVE=dev

# JWT (development only - change in production!)
JWT_SECRET=c21hcnRmYWN0b3J5and0c2VjcmV0a2V5Zm9ydGVzdGluZ29ubHltdXN0YmVjaGFuZ2VkaW4=
JWT_EXPIRATION=86400000

# Frontend
VITE_API_URL=http://localhost:8080

# Development Users (all passwords: password123)
# admin / admin123 (ADMIN)
# prod_worker / password123 (PRODUCTION_WORKER)
# maint_worker / password123 (MAINTENANCE_WORKER)
# quality_worker / password123 (QUALITY_WORKER)
